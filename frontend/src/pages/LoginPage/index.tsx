import LoginForm from '@/features/login/components/LoginForm';

const LoginPage = () => {
  return (
    <div className="flex p-5 items-center justify-center flex-1">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
